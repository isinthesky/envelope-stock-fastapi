# Action Queue

야간 자동화 파이프라인(`stock-pipe` S1)이 매일 04:15에 읽는 작업 큐다.

## 동작 방식

S1은 이 파일의 **맨 위 미완료 항목 하나**를 골라 작업한다. 파일이 없거나 처리할
항목이 없으면 코드베이스에서 안전한 개선(실패·누락 테스트, TODO/FIXME 주석, 좁은
범위의 결함)을 스스로 골라 진행하고, 그것도 없으면 `no_change`로 정상 종료한다.

즉 **이 파일이 비어 있어도 파이프라인은 정상 동작한다.** 특정 작업을 먼저 시키고
싶을 때만 항목을 추가하면 된다.

## 작성 규칙

- 한 항목은 **3파일·200줄 이내**로 끝날 수 있는 크기로 쪼갠다. 예산을 넘으면
  S1이 검증 단계(`change budget exceeded`)에서 실패한다.
- 변경 대상 경로와 완료 조건을 함께 적는다. S1은 `target_test`로 검증할 테스트
  경로를 하나 지정해야 하므로, 관련 테스트가 어디인지도 적어두면 성공률이 오른다.
- 다음은 파이프라인이 **거부하도록 설계된 작업**이라 큐에 넣어도 실패한다:
  커밋·푸시·배포, 서비스 재시작, 자격증명 편집, 주문 실행, 인프라 변경,
  Docker 명령 직접 실행(검증은 스케줄러가 외부에서 수행한다).

## 형식

```markdown
- [ ] <한 줄 요약>
      - 대상: src/application/domain/...
      - 완료 조건: ...
      - 관련 테스트: tests/domain/test_....py
```

## 큐

<!-- 항목을 여기에 추가한다. 비어 있으면 S1이 스스로 개선 대상을 고른다. -->
